# Defense-in-depth against npm supply-chain attacks: refuse install lifecycle
# scripts. Re-enable selectively per package via dependenciesMeta if a
# legitimate native build is required.
enableScripts: false
